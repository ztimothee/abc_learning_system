import 'package:flutter/material.dart';

class StudentAccountLedgersScreen extends StatelessWidget {
  const StudentAccountLedgersScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
